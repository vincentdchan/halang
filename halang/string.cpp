#include "stdafx.h"
#include <cstring>
#include "string.h"

namespace halang
{

	/*
	unsigned int IString::calculateHash(const char* s)
	{
		unsigned int _hash = 5381;
		int c = 0;

		while ((c = *s++))
			_hash = ((_hash << 5) + _hash) + c;
		return _hash;
	}
	*/

};
